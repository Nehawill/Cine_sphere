# Cine_sphere
This notebook builds a movie recommendation system. It uses K-Means clustering to categorize users into 'taste tribes' based on their genre preferences, and then trains a Random Forest Regressor to predict movie ratings."
